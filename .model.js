const model = loader.model; 
class Car extends model { 
} 
const model = loader.model; 
class Car extends model { 
} 
const model = loader.model; 
class Car extends model { 
} 
const model = loader.model; 
class Car extends model { 
} 
const model = loader.model; 
class Car extends model { 
} 
const model = loader.model; 
class Car extends model { 
} 
const model = loader.model; 
class Car extends model { 
} 
