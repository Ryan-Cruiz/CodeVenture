class Fairness {
    // a method that simply returns true if a & b is equal, false otherwise.
    is_fair(a, b) {
        if(a === b){
            return true;
        }
        return false;
    }
}
module.exports = Fairness;